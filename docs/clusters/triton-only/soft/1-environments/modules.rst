.. _t-modules:

Triton Software Modules
================

A list of software modules available on the T2 Triton server can be obtained by running the ``module avail`` command.

The ``module avail`` command reports only the modules that are available with with the current compiler.  The ``module spider`` command lists all available modules, regardless of which compiler is loaded. 

The open-ce module environments provide additional software via taylored Power9 arch repositories. 
